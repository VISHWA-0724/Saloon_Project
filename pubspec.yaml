name: salonease
description: Premium salon booking app (SalonEase).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  dio: ^5.7.0
  shared_preferences: ^2.3.2
  cached_network_image: ^3.4.1
  cupertino_icons: ^1.0.8
  flutter_iconly: ^1.0.2
  google_fonts: ^6.2.1
  intl: ^0.19.0
  lottie: ^3.1.3
  dotted_border: ^2.1.0
  smooth_page_indicator: ^1.2.0+3
  shimmer: ^3.0.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

